# nc-smoke-20260413

## Change

- Added `PUBLICATION_SMOKE_NOTE` to `submission/agent.py` and appended it to the starter `seed_prompt`.
- Left the baseline cheap closed-model settings unchanged: `gpt-4.1-nano`, `plan_horizon: 4`, `policy_decision_interval: 8`.

## Verification

- Ran `submission/agent.py eval` on a lightweight public-train slice using `episode_ids=[2]`.
- The run completed cleanly after `uv sync --extra overcookedv2`.
- Result on the patched prompt:
  - `cross_play_mean_reward`: `20.0`
  - `primary_score`: `20.0`
  - `num_eval_episodes`: `1`
  - `failed_episodes`: `[]`

## Caveat

- This smoke note is behavior-neutral on the sampled slice. It is a publication-oriented prompt marker, not a benchmark improvement.
- The eval is intentionally lightweight: one public-train episode, no self-play, no GIF output.
